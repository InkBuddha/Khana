<% provide(:title, @user.name) %>

<div id="show_user" class="content user_info">
		
			<h2><%= @user.name %></h2>

			<aside>
				<% if @user.recipes.any? %>
					<h4>Last recipes</h4>
					<%= @user.recipes.count %>
					<%#= render @recipes  %>
				<% end %>
			</aside>

</div>

